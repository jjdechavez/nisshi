<header class="bg-white shadow">
  <div class="{{ $slots.action ? 'flex justify-between items-center' : '' }} mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:px-8">
    <h1 class="text-3xl font-bold tracking-tight text-gray-900">{{ title }}</h1>
    @if($slots.action)
      <div class="inline-flex gap-x-4">
        {{{ await $slots.action() }}}
      </div>
    @end
  </div>
</header>
