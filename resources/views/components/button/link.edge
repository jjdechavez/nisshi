@set('classes', 'text-indigo-600 font-medium decoration-2 hover:underline rounded text-sm px-5 py-1.5 text-center inline-block cursor-pointer')

@if(className)
  @set('classes', `${classes} ${className}`)
@end

@!button.primary({ ...$props, classes })
