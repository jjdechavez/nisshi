@layout('layouts/dashboard')

@section('header')
  @!dashboard.header({ title: 'Dashboard' })
@end

@section('content')
  <div class="max-w-[85rem] px-4 py-10 sm:px-6 lg:px-8 lg:py-14 mx-auto">
    <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-4 sm:gap-6">

      @stat.base()
        @!stat.header({ title: 'Total clients' })
        @!stat.body({ count: counts.clients, percent: '1.7', percentType: 'inc' })
      @end

      @stat.base()
        @!stat.header({ title: 'Total projects' })
        @!stat.body({ count: counts.projects })
      @end

      @stat.base()
        @!stat.header({ title: 'Total members' })
        @!stat.body({ count: counts.members })
      @end

    </div>
  </div>
@end
